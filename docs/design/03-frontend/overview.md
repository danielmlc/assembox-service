# 前端层设计

> **状态**: 设计中
> **更新日期**: 2025-01-23

---

## 目录

1. [概述](#1-概述)
2. [整体架构](#2-整体架构)
3. [模块划分](#3-模块划分)
4. [数据流设计](#4-数据流设计)
5. [技术选型](#5-技术选型)
6. [与现有代码的关系](#6-与现有代码的关系)
7. [设计决策记录与相关文档](#7-设计决策记录与相关文档)

---

## 1. 概述

### 1.1 设计目标

前端层是 Assembox 低代码平台的用户交互层，负责将后端的元数据配置渲染为可交互的用户界面。

**核心目标：**

| 目标 | 说明 |
|-----|------|
| 元数据驱动渲染 | 根据 JSON 配置动态生成 UI，无需编写代码 |
| 多端支持 | 一套配置，同时支持 PC 端和移动端渲染 |
| 高性能 | 支持大规模组件树的高效渲染和更新 |
| 可扩展 | 支持自定义组件、自定义渲染逻辑 |
| 开发友好 | 提供完善的类型支持和调试工具 |

### 1.2 核心职责

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            前端层核心职责                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 配置加载与解析                                                           │
│     ├── 从服务端获取页面/组件配置                                            │
│     ├── 解析 JSON Schema 为组件树                                           │
│     └── 处理配置继承与合并                                                   │
│                                                                             │
│  2. 组件渲染                                                                │
│     ├── 根据组件类型选择对应渲染器                                           │
│     ├── 处理组件属性绑定                                                    │
│     └── 管理组件生命周期                                                    │
│                                                                             │
│  3. 数据管理                                                                │
│     ├── 管理页面级数据状态                                                  │
│     ├── 处理数据双向绑定                                                    │
│     └── 与服务端数据同步                                                    │
│                                                                             │
│  4. 事件处理                                                                │
│     ├── 响应用户交互事件                                                    │
│     ├── 执行配置中的事件逻辑                                                │
│     └── 组件间通信                                                          │
│                                                                             │
│  5. 路由管理                                                                │
│     ├── 动态路由生成                                                        │
│     ├── 页面导航控制                                                        │
│     └── 路由参数传递                                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 设计原则

1. **配置即 UI (Configuration as UI)**
   - UI 完全由配置驱动
   - 配置变更即时反映到界面

2. **渲染器与组件分离**
   - 渲染逻辑与具体组件解耦
   - 支持多套 UI 组件库

3. **统一运行时**
   - PC 端和移动端共享核心运行时
   - 仅渲染器和组件库不同

4. **响应式数据流**
   - 基于 Vue 3 响应式系统
   - 单向数据流 + 事件上报

---

## 2. 整体架构

### 2.1 分层架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              应用层                                          │
│                     (宿主应用 / 设计器预览)                                   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            渲染器层                                          │
├─────────────────────────────────┬───────────────────────────────────────────┤
│         PC端渲染器               │           移动端渲染器                     │
│      (assembox-pc)              │        (assembox-mobile)                  │
│                                 │                                           │
│  ┌─────────────────────────┐   │   ┌─────────────────────────┐             │
│  │   Element Plus 组件     │   │   │     NutUI 组件          │             │
│  │   ├── 表单组件          │   │   │     ├── 表单组件        │             │
│  │   ├── 表格组件          │   │   │     ├── 列表组件        │             │
│  │   ├── 布局组件          │   │   │     ├── 布局组件        │             │
│  │   └── 业务组件          │   │   │     └── 业务组件        │             │
│  └─────────────────────────┘   │   └─────────────────────────┘             │
└─────────────────────────────────┴───────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           核心运行时                                         │
│                      (assembox-runtime)                                     │
│                                                                             │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐   │
│  │  配置解析器    │ │   渲染引擎    │ │  状态管理器    │ │   事件总线    │   │
│  │ ConfigParser  │ │ RenderEngine │ │ StateManager │ │  EventBus    │   │
│  └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘   │
│                                                                             │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐   │
│  │  表达式引擎    │ │   数据绑定    │ │   路由管理    │ │   权限控制    │   │
│  │ ExprEngine   │ │ DataBinding  │ │ RouterMgr    │ │  AuthGuard   │   │
│  └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            API调用层                                         │
│                        (assembox-api)                                       │
│                                                                             │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐   │
│  │  请求封装      │ │   响应处理    │ │   错误处理    │ │   缓存管理    │   │
│  │ RequestUtil  │ │ ResponseUtil │ │ ErrorHandler │ │  CacheMgr    │   │
│  └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼ HTTP/WebSocket
                         ┌─────────────────────┐
                         │      服务端         │
                         │  (assembox-server)  │
                         └─────────────────────┘
```

### 2.2 模块依赖关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            模块依赖关系                                      │
└─────────────────────────────────────────────────────────────────────────────┘

                        assembox-types (类型定义)
                               ▲
          ┌────────────────────┼────────────────────┐
          │                    │                    │
          │                    │                    │
    assembox-api ◀──── assembox-runtime ────▶ (第三方依赖)
    (API调用)             ▲    ▲              Vue 3
                          │    │              VueRouter
          ┌───────────────┘    └───────────────┐
          │                                    │
          │                                    │
    assembox-pc                          assembox-mobile
    (PC渲染器)                            (移动端渲染器)
          │                                    │
          ▼                                    ▼
    Element Plus                            NutUI
    (PC组件库)                            (移动组件库)
```

### 2.3 包职责说明

| 包名 | 职责 | 依赖 |
|-----|------|------|
| assembox-types | 统一类型定义 | 无 |
| assembox-api | API 请求封装、响应处理 | assembox-types, axios |
| assembox-runtime | 核心运行时逻辑 | assembox-types, assembox-api, vue |
| assembox-pc | PC 端渲染器和组件 | assembox-runtime, element-plus |
| assembox-mobile | 移动端渲染器和组件 | assembox-runtime, nutui |

---

## 3. 模块划分

### 3.1 assembox-types

**统一类型定义包**，被所有前端包依赖。

```
assembox-types/
├── src/
│   ├── index.ts                    # 导出入口
│   │
│   ├── node/                       # 节点类型定义
│   │   ├── base-node.ts            # 基础节点接口
│   │   ├── render-node.ts          # 渲染节点
│   │   ├── layout-node.ts          # 布局节点
│   │   └── index.ts
│   │
│   ├── config/                     # 配置类型定义
│   │   ├── page-config.ts          # 页面配置
│   │   ├── component-config.ts     # 组件配置
│   │   ├── datasource-config.ts    # 数据源配置
│   │   ├── event-config.ts         # 事件配置
│   │   └── index.ts
│   │
│   ├── context/                    # 上下文类型
│   │   ├── render-context.ts       # 渲染上下文
│   │   ├── global-context.ts       # 全局上下文
│   │   └── index.ts
│   │
│   └── component/                  # 组件类型
│       ├── props.ts                # 组件属性类型
│       ├── events.ts               # 事件类型
│       └── index.ts
│
├── package.json
└── tsconfig.json
```

### 3.2 assembox-api

**API 调用管理包**，封装与服务端的通信逻辑。

```
assembox-api/
├── src/
│   ├── index.ts                    # 导出入口
│   │
│   ├── core/                       # 核心功能
│   │   ├── request.ts              # 请求封装
│   │   ├── response.ts             # 响应处理
│   │   ├── interceptor.ts          # 拦截器
│   │   └── index.ts
│   │
│   ├── services/                   # API 服务
│   │   ├── config-service.ts       # 配置加载服务
│   │   ├── data-service.ts         # 数据操作服务
│   │   ├── auth-service.ts         # 认证服务
│   │   └── index.ts
│   │
│   ├── cache/                      # 缓存管理
│   │   ├── config-cache.ts         # 配置缓存
│   │   ├── data-cache.ts           # 数据缓存
│   │   └── index.ts
│   │
│   └── utils/                      # 工具函数
│       ├── url-builder.ts          # URL 构建
│       ├── params-builder.ts       # 参数构建
│       └── index.ts
│
├── package.json
└── tsconfig.json
```

### 3.3 assembox-runtime

**核心运行时包**，提供配置解析、渲染引擎、状态管理等核心能力。

```
assembox-runtime/
├── src/
│   ├── index.ts                    # 导出入口
│   │
│   ├── parser/                     # 配置解析
│   │   ├── config-parser.ts        # 配置解析器
│   │   ├── schema-validator.ts     # Schema 验证
│   │   ├── node-builder.ts         # 节点构建器
│   │   └── index.ts
│   │
│   ├── engine/                     # 渲染引擎
│   │   ├── render-engine.ts        # 渲染引擎核心
│   │   ├── component-resolver.ts   # 组件解析器
│   │   ├── slot-manager.ts         # 插槽管理
│   │   └── index.ts
│   │
│   ├── state/                      # 状态管理
│   │   ├── state-manager.ts        # 状态管理器
│   │   ├── data-model.ts           # 数据模型
│   │   ├── reactive-bridge.ts      # 响应式桥接
│   │   └── index.ts
│   │
│   ├── event/                      # 事件系统
│   │   ├── event-bus.ts            # 事件总线
│   │   ├── event-handler.ts        # 事件处理器
│   │   ├── event-registry.ts       # 事件注册表
│   │   └── index.ts
│   │
│   ├── expression/                 # 表达式引擎
│   │   ├── expr-parser.ts          # 表达式解析
│   │   ├── expr-evaluator.ts       # 表达式求值
│   │   ├── expr-context.ts         # 表达式上下文
│   │   └── index.ts
│   │
│   ├── binding/                    # 数据绑定
│   │   ├── data-binding.ts         # 数据绑定核心
│   │   ├── two-way-binding.ts      # 双向绑定
│   │   ├── computed-binding.ts     # 计算绑定
│   │   └── index.ts
│   │
│   ├── router/                     # 路由管理
│   │   ├── router-manager.ts       # 路由管理器
│   │   ├── dynamic-route.ts        # 动态路由
│   │   ├── route-guard.ts          # 路由守卫
│   │   └── index.ts
│   │
│   ├── auth/                       # 权限控制
│   │   ├── auth-manager.ts         # 权限管理器
│   │   ├── permission-checker.ts   # 权限检查
│   │   └── index.ts
│   │
│   ├── lifecycle/                  # 生命周期
│   │   ├── lifecycle-manager.ts    # 生命周期管理
│   │   ├── hooks.ts                # 生命周期钩子
│   │   └── index.ts
│   │
│   └── utils/                      # 工具函数
│       ├── deep-merge.ts           # 深度合并
│       ├── path-resolver.ts        # 路径解析
│       └── index.ts
│
├── package.json
└── tsconfig.json
```

### 3.4 assembox-pc

**PC 端渲染器包**，基于 Element Plus 实现。

```
assembox-pc/
├── src/
│   ├── index.ts                    # 导出入口
│   │
│   ├── renderer/                   # 渲染器
│   │   ├── pc-renderer.ts          # PC渲染器
│   │   ├── component-map.ts        # 组件映射表
│   │   └── index.ts
│   │
│   ├── components/                 # 组件实现
│   │   ├── layout/                 # 布局组件
│   │   │   ├── Container.vue       # 容器
│   │   │   ├── Row.vue             # 行
│   │   │   ├── Col.vue             # 列
│   │   │   ├── Card.vue            # 卡片
│   │   │   └── index.ts
│   │   │
│   │   ├── form/                   # 表单组件
│   │   │   ├── Form.vue            # 表单
│   │   │   ├── FormItem.vue        # 表单项
│   │   │   ├── Input.vue           # 输入框
│   │   │   ├── Select.vue          # 下拉框
│   │   │   ├── DatePicker.vue      # 日期选择器
│   │   │   └── index.ts
│   │   │
│   │   ├── table/                  # 表格组件
│   │   │   ├── Table.vue           # 表格
│   │   │   ├── TableColumn.vue     # 表格列
│   │   │   ├── TableEdit.vue       # 可编辑表格
│   │   │   └── index.ts
│   │   │
│   │   ├── display/                # 展示组件
│   │   │   ├── Label.vue           # 标签
│   │   │   ├── Image.vue           # 图片
│   │   │   ├── Tag.vue             # 标签
│   │   │   └── index.ts
│   │   │
│   │   ├── action/                 # 操作组件
│   │   │   ├── Button.vue          # 按钮
│   │   │   ├── Dropdown.vue        # 下拉菜单
│   │   │   └── index.ts
│   │   │
│   │   ├── feedback/               # 反馈组件
│   │   │   ├── Dialog.vue          # 对话框
│   │   │   ├── Drawer.vue          # 抽屉
│   │   │   └── index.ts
│   │   │
│   │   └── business/               # 业务组件
│   │       ├── DictSelect.vue      # 字典选择
│   │       ├── OrgTree.vue         # 组织树
│   │       ├── Upload.vue          # 文件上传
│   │       └── index.ts
│   │
│   ├── composables/                # 组合式函数
│   │   ├── useForm.ts              # 表单逻辑
│   │   ├── useTable.ts             # 表格逻辑
│   │   ├── useDialog.ts            # 对话框逻辑
│   │   └── index.ts
│   │
│   └── styles/                     # 样式
│       ├── variables.scss          # 变量
│       ├── mixins.scss             # 混入
│       └── index.scss
│
├── package.json
└── tsconfig.json
```

### 3.5 assembox-mobile

**移动端渲染器包**，基于 NutUI 实现。

```
assembox-mobile/
├── src/
│   ├── index.ts                    # 导出入口
│   │
│   ├── renderer/                   # 渲染器
│   │   ├── mobile-renderer.ts      # 移动端渲染器
│   │   ├── component-map.ts        # 组件映射表
│   │   └── index.ts
│   │
│   ├── components/                 # 组件实现
│   │   ├── layout/                 # 布局组件
│   │   │   ├── Page.vue            # 页面容器
│   │   │   ├── Cell.vue            # 单元格
│   │   │   ├── CellGroup.vue       # 单元格组
│   │   │   └── index.ts
│   │   │
│   │   ├── form/                   # 表单组件
│   │   │   ├── Form.vue            # 表单
│   │   │   ├── Input.vue           # 输入框
│   │   │   ├── Picker.vue          # 选择器
│   │   │   ├── DatePicker.vue      # 日期选择
│   │   │   └── index.ts
│   │   │
│   │   ├── list/                   # 列表组件
│   │   │   ├── List.vue            # 列表
│   │   │   ├── ListItem.vue        # 列表项
│   │   │   ├── PullRefresh.vue     # 下拉刷新
│   │   │   └── index.ts
│   │   │
│   │   ├── display/                # 展示组件
│   │   │   ├── Text.vue            # 文本
│   │   │   ├── Image.vue           # 图片
│   │   │   ├── Tag.vue             # 标签
│   │   │   └── index.ts
│   │   │
│   │   ├── action/                 # 操作组件
│   │   │   ├── Button.vue          # 按钮
│   │   │   ├── ActionSheet.vue     # 动作面板
│   │   │   └── index.ts
│   │   │
│   │   └── business/               # 业务组件
│   │       ├── DictPicker.vue      # 字典选择
│   │       ├── Signature.vue       # 签名
│   │       ├── Upload.vue          # 文件上传
│   │       └── index.ts
│   │
│   ├── composables/                # 组合式函数
│   │   ├── useForm.ts              # 表单逻辑
│   │   ├── useList.ts              # 列表逻辑
│   │   └── index.ts
│   │
│   └── styles/                     # 样式
│       ├── variables.scss          # 变量
│       └── index.scss
│
├── package.json
└── tsconfig.json
```

---

## 4. 数据流设计

### 4.1 整体数据流

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            前端数据流架构                                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│  用户交互         │
│  (User Action)   │
└────────┬─────────┘
         │ ① 触发事件
         ▼
┌──────────────────┐     ② 查找处理器    ┌──────────────────┐
│    事件总线       │ ─────────────────▶ │   事件处理器      │
│   (EventBus)     │                    │ (EventHandler)   │
└────────┬─────────┘                    └────────┬─────────┘
         │                                       │
         │ ③ 事件响应                            │ ④ 执行逻辑
         ▼                                       ▼
┌──────────────────┐                    ┌──────────────────┐
│   组件更新        │ ◀──────────────── │   状态管理器      │
│  (Component)     │     ⑤ 状态变更    │ (StateManager)   │
└──────────────────┘                    └────────┬─────────┘
                                                 │
                                                 │ ⑥ 数据请求
                                                 ▼
                                        ┌──────────────────┐
                                        │    API 层        │
                                        │ (assembox-api)   │
                                        └────────┬─────────┘
                                                 │
                                                 │ ⑦ HTTP 请求
                                                 ▼
                                        ┌──────────────────┐
                                        │     服务端       │
                                        └──────────────────┘
```

### 4.2 配置加载流程

```
┌──────────────────┐
│   应用启动        │
└────────┬─────────┘
         │ ① 请求页面配置
         ▼
┌──────────────────┐     ② 获取配置     ┌──────────────────┐
│   配置服务        │ ────────────────▶ │     服务端        │
│ (ConfigService)  │                   │                  │
└────────┬─────────┘ ◀──────────────── └──────────────────┘
         │              ③ 返回 JSON
         │
         │ ④ 解析配置
         ▼
┌──────────────────┐
│   配置解析器      │
│ (ConfigParser)   │
└────────┬─────────┘
         │ ⑤ 生成节点树
         ▼
┌──────────────────┐
│   节点构建器      │
│ (NodeBuilder)    │
└────────┬─────────┘
         │ ⑥ 传递节点树
         ▼
┌──────────────────┐
│   渲染引擎        │
│ (RenderEngine)   │
└────────┬─────────┘
         │ ⑦ 渲染组件
         ▼
┌──────────────────┐
│   页面 UI        │
└──────────────────┘
```

### 4.3 状态管理设计

```typescript
/**
 * 页面状态结构
 */
interface PageState {
  // 页面配置
  config: PageConfig;

  // 数据模型
  dataModel: Record<string, any>;

  // 组件状态
  componentStates: Record<string, ComponentState>;

  // 加载状态
  loading: Record<string, boolean>;

  // 上下文信息
  context: GlobalContext;
}

/**
 * 组件状态
 */
interface ComponentState {
  // 组件值
  value: any;

  // 可见性
  visible: boolean;

  // 禁用状态
  disabled: boolean;

  // 验证状态
  validation: ValidationState;
}
```

### 4.4 事件系统设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            事件系统架构                                      │
└─────────────────────────────────────────────────────────────────────────────┘

事件类型分类：

┌────────────────┐  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐
│  交互事件       │  │  生命周期事件   │  │  数据事件       │  │  系统事件       │
├────────────────┤  ├────────────────┤  ├────────────────┤  ├────────────────┤
│ onClick        │  │ onMounted      │  │ onChange       │  │ onError        │
│ onFocus        │  │ onUnmounted    │  │ onLoadData     │  │ onResize       │
│ onBlur         │  │ onInit         │  │ onSaveData     │  │ onRouteChange  │
│ onSelect       │  │ onShow         │  │ onValidate     │  │ onPermChange   │
└────────────────┘  └────────────────┘  └────────────────┘  └────────────────┘

事件处理流程：

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  事件触发     │ ──▶ │  事件分发     │ ──▶ │  处理器执行   │
│  (Emit)      │     │  (Dispatch)  │     │  (Execute)   │
└──────────────┘     └──────────────┘     └──────────────┘
                            │
                            ▼
                     ┌──────────────┐
                     │  事件注册表   │
                     │  (Registry)  │
                     │              │
                     │ ┌──────────┐ │
                     │ │ 系统处理器│ │
                     │ └──────────┘ │
                     │ ┌──────────┐ │
                     │ │ 自定义处理│ │
                     │ └──────────┘ │
                     └──────────────┘
```

---

## 5. 技术选型

### 5.1 核心技术栈

| 技术 | 版本 | 用途 | 说明 |
|-----|------|-----|------|
| Vue | 3.4+ | 核心框架 | Composition API、响应式系统 |
| TypeScript | 5.0+ | 类型系统 | 全量类型覆盖 |
| Vite | 5.0+ | 构建工具 | 开发服务器、打包 |
| VueRouter | 4.x | 路由管理 | 动态路由 |
| Pinia | 2.x | 状态管理 | 可选，复杂场景使用 |

### 5.2 UI 组件库

| 端 | 组件库 | 版本 | 说明 |
|---|--------|-----|------|
| PC | Element Plus | latest | 企业级 UI 组件库 |
| Mobile | NutUI | 4.x | 京东移动端组件库 |

### 5.3 工具库

| 库 | 用途 |
|---|------|
| axios | HTTP 请求 |
| lodash-es | 工具函数 |
| dayjs | 日期处理 |
| nanoid | ID 生成 |

---

## 6. 与现有代码的关系

### 6.1 代码复用计划

| 现有模块 | 新模块 | 复用方式 |
|---------|-------|---------|
| assembox-core/type | assembox-types | 迁移并扩展类型定义 |
| assembox-core/request | assembox-api | 重构请求逻辑 |
| assembox-desktop | assembox-pc | 重构渲染器，复用组件 |
| assembox-mobile | assembox-mobile | 重构渲染器，复用组件 |

### 6.2 需要重构的部分

| 模块 | 重构原因 | 重构方向 |
|-----|---------|---------|
| 渲染器 | 组件与渲染逻辑耦合 | 分离渲染引擎和组件实现 |
| 事件系统 | 事件处理分散 | 集中事件总线管理 |
| 状态管理 | 状态管理不统一 | 统一状态管理方案 |
| 类型定义 | 类型不完整 | 完善类型覆盖 |

### 6.3 迁移策略

```
阶段1: 基础设施
├── 建立新的包结构
├── 迁移类型定义
└── 建立核心运行时框架

阶段2: 核心能力
├── 实现配置解析器
├── 实现渲染引擎
├── 实现状态管理
└── 实现事件系统

阶段3: 渲染器迁移
├── PC 渲染器重构
├── 组件迁移和适配
└── 移动端渲染器重构

阶段4: 集成验证
├── 与设计器集成
├── 功能回归测试
└── 性能优化
```

---

## 7. 设计决策记录与相关文档

### 7.1 设计决策记录

| 问题 | 决策 | 说明 |
|-----|------|------|
| 运行时架构 | 统一运行时 + 多端渲染器 | PC/Mobile 共享核心运行时，仅渲染器不同 |
| 状态管理 | 内置轻量状态管理 | 不强依赖 Pinia，提供可选集成 |
| 组件通信 | 事件总线 | 统一事件分发，支持跨组件通信 |
| 表达式引擎 | 安全沙箱执行 | 支持配置中的动态表达式，安全隔离 |
| 数据绑定 | 路径绑定 | 通过路径字符串实现数据绑定 |
| 组件映射 | 配置驱动 | 组件类型到实际组件的映射可配置 |

### 7.2 相关文档

| 序号 | 文档 | 状态 | 主要内容 |
|:---:|-----|:----:|---------|
| 1 | [核心运行时设计](./runtime.md) | 📝 | 配置解析、渲染引擎、状态管理、事件系统 |
| 2 | [PC端渲染器设计](./pc-renderer.md) | 📝 | PC渲染器实现、组件映射、样式处理 |
| 3 | [移动端渲染器设计](./mobile-renderer.md) | 📝 | 移动端渲染器、适配方案 |
| 4 | [组件库设计](./components.md) | 📝 | 组件分类、接口规范、扩展机制 |

> 图例: ✅ 已完成 | 📝 待编写

---
