# Assembox 低代码平台 - 后端服务

基于 NestJS + TypeScript 的微服务架构，使用 pnpm monorepo 管理。

## 🚀 快速开始

```bash
# 1. 安装依赖
pnpm bootstrap

# 2. 启动存储层服务
pnpm dev:storage

# 3. 访问服务、
# http://localhost:3000
```

## 📦 项目结构

```
├── lib/              # 云平台基础设施包源码（仅供参考）
├── projects/          # 后端业务服务 🎯
│   └── assembox-storage/   # 存储层服务
├── packages/         # 前端项目
└── docs/            # 文档
```

**注意**：`lib/` 目录包含 `@cs/*` 包的源码，仅供代码阅读参考。实际使用时请安装发布的 npm 包版本。

## 🔨 常用命令

### 开发

```bash
pnpm dev:storage        # 启动存储层服务（开发模式）
pnpm build:storage      # 构建存储层服务
pnpm start:storage      # 启动存储层服务（生产模式）
```

### 代码质量

```bash
pnpm lint:backend       # 代码检查
pnpm format            # 代码格式化
pnpm typecheck         # 类型检查
```

### 工作区管理

```bash
pnpm ws:list           # 查看所有包
pnpm clean             # 清理所有构建产物
pnpm build:all         # 构建所有包
```

## 📚 详细文档

查看 [MONOREPO.md](./MONOREPO.md) 了解完整的开发指南。

## 🛠️ 技术栈

- **框架**: NestJS + TypeScript
- **数据库**: TiDB (兼容 MySQL)
- **缓存**: Redis
- **存储**: OSS
- **配置**: Nacos
- **包管理**: pnpm workspace

## 🏗️ 服务架构

### assembox-storage (存储层服务)

配置管理和存储服务，支持三层配置继承（system → global → tenant）。

**核心功能：**
- 模块版本管理
- 组件注册
- 配置草稿/发布
- 配置继承查找
- 三层缓存策略
- OSS 存储

**API 文档：** 查看 [projects/assembox-storage/README.md](./projects/assembox-storage/README.md)

## 📝 开发规范

详见 [CLAUDE.md](./CLAUDE.md)

## 📄 License

ISC
